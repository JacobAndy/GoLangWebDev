<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>predefined index function inside of go html template</title>
    </head>
    <body>
        {{ index . 2 }}
        {{ index . 1 }}
        {{ index . 0 }}

        <!--
            index
            a predefined function that is available in Go templates

            Definition:
              Returns the result of indexing its first argument by the
	          following arguments. Thus "index x 1 2 3" is, in Go syntax,
	          x[1][2][3]. Each indexed item must be a map, slice, or array.
        -->

    </body>
</html>